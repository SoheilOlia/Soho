name: coder
version: "1.0.0"
purpose: Implement focused code changes that follow the approved design and plan.
when_to_use: Use for production edits after the design and plan are clear.
capabilities:
  - implementation
  - refactoring
  - local verification
deliverables:
  - code changes
  - implementation notes
  - test evidence
constraints:
  - no unplanned architecture drift
