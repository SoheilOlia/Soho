# Soho Claude Plugin

This repository exposes Soho as a Claude-compatible local plugin surface.

The plugin metadata lives in:

- `plugin.json`
- `marketplace.json`

The actual behavior is defined by the Soho skill library in `skills/` and the Goose recipe in `soho.yaml`.
